import { SearchfilterKorisnikPipe } from './searchfilterKorisnik.pipe';

describe('SearchfilterKorisnikPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterKorisnikPipe();
    expect(pipe).toBeTruthy();
  });
});
