import { SearchfilterPisacPipe } from './searchfilterPisac.pipe';

describe('SearchfilterPisacPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterPisacPipe();
    expect(pipe).toBeTruthy();
  });
});
