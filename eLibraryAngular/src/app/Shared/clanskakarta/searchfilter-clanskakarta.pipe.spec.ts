import { SearchfilterClanskakartaPipe } from './searchfilter-clanskakarta.pipe';

describe('SearchfilterClanskakartaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterClanskakartaPipe();
    expect(pipe).toBeTruthy();
  });
});
