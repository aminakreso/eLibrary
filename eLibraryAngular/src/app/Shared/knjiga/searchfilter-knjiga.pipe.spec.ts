import { SearchfilterKnjigaPipe } from './searchfilter-knjiga.pipe';

describe('SearchfilterKnjigaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterKnjigaPipe();
    expect(pipe).toBeTruthy();
  });
});
