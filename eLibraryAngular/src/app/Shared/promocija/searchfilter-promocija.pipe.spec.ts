import { SearchfilterPromocijaPipe } from './searchfilter-promocija.pipe';

describe('SearchfilterPromocijaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterPromocijaPipe();
    expect(pipe).toBeTruthy();
  });
});

