import { SearchfilterUplataPipe } from './searchfilter-uplata.pipe';

describe('SearchfilterUplataPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterUplataPipe();
    expect(pipe).toBeTruthy();
  });
});
